<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
  <link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
/>

  <style>
    body {
      margin: 0;
      padding: 20px;
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: #fff;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .blog-container {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(6px);
      border-radius: 15px;
      padding: 30px;
      max-width: 700px;
      width: 100%;
      box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
      text-align: center;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
    }

    .content {
      font-size: 1.1rem;
      line-height: 1.6;
      margin-bottom: 20px;
      color: #f0f0f0;
      text-align: justify;
    }

    p {
      font-style: italic;
      margin-bottom: 20px;
      color: #ddd;
    }

    a {
      display: inline-block;
      text-decoration: none;
      color: #fff;
      padding: 10px 20px;
      margin: 5px;
      border-radius: 20px;
      font-weight: bold;
      transition: 0.3s;
    }

    .Edit {
      background: #36d1dc;
    }

    .profile {
      background: #764ba2;
    }

    a:hover {
      opacity: 0.9;
      transform: scale(1.05);
    }
    .like-section {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-bottom: 20px;
}

.like-btn {
  background: rgba(255, 255, 255, 0.15);
  border: none;
  border-radius: 50%;
  width: 45px;
  height: 45px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.25s ease;
  backdrop-filter: blur(6px);
}

.like-btn i {
  font-size: 20px;
  color: #fff;
  transition: transform 0.2s ease, color 0.2s ease;
}

.like-btn:hover {
  background: rgba(255, 255, 255, 0.25);
  transform: scale(1.08);
}

.like-btn:active i {
  transform: scale(1.3);
}

.like-count {
  font-size: 1rem;
  font-weight: bold;
  color: #fff;
}

  </style>
</head>
<body>
<div class="blog-container">
    <h1><%= post.postTittle %></h1>
    <div class="content"><%= post.postData %></div>
    <p>- by <%= user.name %></p>
  <div class="like-section" data-liked="<%= likedByUser %>">
    <button class="like-btn" type="button">
  <% if (likedByUser) { %>
    <i class="fa-solid fa-heart"></i>
  <% } else { %>
    <i class="fa-regular fa-heart"></i>
  <% } %>
</button>
<span class="like-count"> <%=post.likes.length%> </span>
  </div>
    <a class="Edit" href="/post/edit/<%= post._id %>">Edit</a>
    <a class="profile" href="/profile">Profile</a>
</div>
<script src="/javascripts/like.js"></script>
</body>
</html>

<%# this one comes fron writeContent %> 
<%# idhar biich mai only do hi routes hai that we know that sort of sort the issue of ours the full page read jaha pe we have the like ka  icon %>

