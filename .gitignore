# Node dependencies
node_modules

# Environment files
.env

# OS or IDE files
.DS_Store
*.log

# Optional: build cache or Vercel metadata
.vercel

# Bench: minted JWTs for local load tests — live credentials, never commit
bench/tokens.json
